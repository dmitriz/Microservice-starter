// functions.js
module.exports = (params) => (onSuccess, onError) => {
  // Access params properties: params.property1, params.property2...
  // Perform your logic here...
  // Call onSuccess with the result in case of success
  // Call onError with an error object in case of failure
};
